#include<stdio.h>
int main()
{
   int a[10][10],b[10][10],t[10][10],i,j,c,r,s[10][10];
   printf("enter the size of row r:");
   scanf("%d",&r);
   printf("enter the size of the column:");
   scanf("%d",&c);
   printf("enter the element of 1st matrix:");
   for(i=0;i<r;i++)
   {
      for(j=0;j<c;j++)
      {
         scanf("%d",&a[i][j]);
      }
   }
   printf("enter the elements of 2nd matrix:");
   for(i=0;i<r;i++)
   {
      for(j=0;j<c;j++)
      {
         scanf("%d",&b[i][j]);
      }
   }
   for(i=0;i<r;i++)
   {
      for(j=0;j<r;j++)
      {
         t[i][j]=a[i][j]+b[i][j];
      }
   }
   printf("\nsum of the matrices is:\n");
   for(i=0;i<r;i++)
   {
      for(j=0;j<c;j++)
      {
         printf("\t%d",t[i][j]);
      }
      printf("\n");}
      for(i=0;i<r;i++)
      {
         for(j=0;j<r;j++)
         {
            s[i][j]=a[i][j]-b[i][j];
         }
         }
      printf("\nsubraction answer is :\n");
      for(i=0;i<r;i++)
      {
         for(j=0;j<c;j++)
         {
            printf("\t%d",s[i][j]);
         }
         printf("\n");
      }
}
