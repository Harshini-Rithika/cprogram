#include<stdio.h>
int main()
{
   int a[10],i,m,n,count=0;
      printf("enter the size of an array n:");
   scanf("%d",&n);
   printf("enter the array elements:");
   for(i=0;i<=n;i++)
   {
      scanf("%d",&a[i]);
   }
   printf("enter any number m:");
   scanf("%d",&m);

   for(i=0;i<=n;i++)
   {
      if(m==a[i])
      {
         printf("\nthe position of the number in the array is %d",i+1);
      }
   }
}
