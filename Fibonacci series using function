#include<stdio.h>
int fibo(int);
int main()
{
   int n,r;
   printf("enter the number:");
   scanf("%d",&n);
   r=fibo(n);
   printf("%d",r);
}
int fibo(int n)
{
   if(n==1)
   {
      return 0;
   }
   else if(n==2)
   {
      return 1;
   }
   else
   {
      return fibo(n-1)+fibo(n-2);
   }
}
