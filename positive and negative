#include<stdio.h>
int main()
{
   int a;
   printf("enter the value of a:");
   scanf("%d",&a);
   if(a<0)
      printf("the number %d is negative",a);
   else
      printf("the number %d is positive",a);
}
