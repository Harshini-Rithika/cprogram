#include<stdio.h>
int main()
{
   int i,n,a=0;
   printf("enter the number n:");
   scanf("%d",&n);
   for(i=2;i<n;i++)
   {
      if(n%i==0)
      {
         a++;
      }
      {
         if(a==0)
         {
            printf("the number is prime number");
         }
         else
         {
            printf("the number is not prime number");
         }
      }
   }
}
